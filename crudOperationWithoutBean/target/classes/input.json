[
  {
    "ModuleName": "Student",
    "Fields": [
      {
        "name": "name",
        "type": "string",
        "validation": {
          "pattern": "^[A-Za-z]+$",
          "maxLength": 20
        }
      },
      {
        "name": "city",
        "type": "string",
        "validation": {
          "pattern": "^[A-Za-z]+$",
          "maxLength": 20
        }
      },
      {
        "name": "age",
        "type": "number",
        "validation": {
          "minimum": 0,
          "maximum": 120
        }
      },
      {
        "name": "gender",
        "validation": {
          "pattern": "^[A-Za-z]$"
        }
      }
    ]
  }
]
